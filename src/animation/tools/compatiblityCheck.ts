export const text = "warning";

// const compatibilityCheck = (WebGL) => {
//   if (WebGL.isWebGLAvailable()) {
//     // animate();
//   } else {
//     const warning = WebGL.getWebGLErrorMessage();
//     // document.getElementById( 'container' ).appendChild( warning );
//   }
// }